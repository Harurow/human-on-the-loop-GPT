# 設計原則

1. 人間が目的と要件を承認し、その範囲を自律的に進める。権限と範囲は既存のユーザー指示を引き継ぐ。
2. 要件と実行範囲を一緒に提示し、承認後の変更は差分と影響を示す。承認を重ねるためのゲートは作らない。
3. 機械的に決まる状態更新は CLI、意味の判断と実装はエージェントが担う。
4. 承認・状態・入力・証跡は一つの JSON で原子的に更新。人間向けログは導出する。
5. 一つのメッセージを複数の intent に分け、入力 ID ごとに処理結果を記録する。
6. 実装者と検証者を分け、対象と証拠のハッシュを結び付ける。独立性の偽装で完了しない。
7. 文書とレビューの量を作業の規模・リスクに合わせ、早い段階で実際の主要操作を確認する。
8. 中断や異常時は既存成果物を保全し、不明な状態を勝手に初期化しない。

## 原版からの変更

参照元: ../human-on-the-loop、commit `6d20f93475f472261cac50c6badb71e348c2d9e9`。
原版のステートマシン、AgentTrail、コンテキスト分離、要件追跡、再開、PM の思想を引き継ぐ。
原版の長い例外手順をそのまま複製せず、CLI の不変条件と短いフェーズ手順へ再構成した。
原版の ADR-009/011 が示すレビュー反復の問題を踏まえ、既知の境界は実行テストで確認する。

原版の tasks.md が進捗の正本である方針は維持する。
ただし log.md の追記専用正本は、state 内の E-n イベント列へ移した。
この変更により状態と証跡の別々の書き込みによる不一致をなくし、log.md を再生成可能にする。
従って原版と state の互換性はなく、既存プロジェクトの自動移行は含めない。

## 初版の境界

Codex 用のローカルスキルであり、ChatGPT のカスタム GPT や独立した常駐エージェントではない。
CLI は LLM/API を呼ばず、承認発言の意味や担当の実在を認証しない。
同一権限のエージェントが JSON を手編集することを防ぐセキュリティ境界ではない。
ファイルはローカル macOS/Linux を想定し、Windows、ネットワークファイルシステム、
サブモジュール、シンボリックリンクを含む検証対象、複数セッションの同時コード編集は対象外。
標準ライブラリの fcntl ロックは CLI の更新競合を防ぐもので、実装作業全体のリースではない。
レビューは通常 code/acceptance を必須とする。独立完了版からの限定的な説明文書修正は
根拠付き editorial 自己検証を認める。UX/セキュリティの追加適用は変更の影響に基づき判断する。
