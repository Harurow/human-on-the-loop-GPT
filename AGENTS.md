# 開発ガイド

このリポジトリは human-on-the-loop-GPT の配布元。自身を開発対象アプリとして初期化しない。
設計の根拠は PRINCIPLES.md。スキル入口は skills/hotl-gpt/SKILL.md。

- 原版 ../human-on-the-loop は参照元。変更しない。
- Python 3.9+ 標準ライブラリで状態管理を行う。承認・入力・イベントは一つの正本で保存する。
- 手順変更時は CLI の契約とスキルを同時に確認する。手順書だけに新しい状態を増やさない。
- 検証は `python3 -m unittest discover -s tests -v` と `python3 scripts/check_consistency.py`。
- 状態管理を変更したら、承認後改変、入力混在、途中終了、古い検証証拠の回帰を含める。
- スキルの実地確認は一時プロジェクトで行い、実施内容と未実施範囲を docs/validation.md に記す。
- 自動テストの合格と、実際の独立エージェント検証を区別する。
- Git 管理されている場合はこの依頼の変更をコミットし、origin があれば push まで行う。
  未管理ならこの規則のためだけに初期化しない。未実施の Git 処理は理由と残タスクを報告する。
- 作業終了時は「作業報告書」として、対象・完了状況・変更点・検証結果・Git の結果・課題/残タスク・必要なユーザー作業を簡潔に提出する。
